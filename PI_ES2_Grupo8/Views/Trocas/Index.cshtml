@model IEnumerable<PI_ES2_Grupo8.Models.Troca>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.justificação)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EnfermeiroId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Data)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HorarioServicoDomicilio)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.justificação)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EnfermeiroId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Data)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HorarioServicoDomicilio.HorarioServicoDomicilioId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TrocaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TrocaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TrocaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
