@model IEnumerable<PI_ES2_Grupo8.Models.ReceitarTratamento>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ReceitarTratamentoId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.receita)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tratamento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ReceitarTratamentoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.receita.ReceitaId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tratamento.TipodeTratamento)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
